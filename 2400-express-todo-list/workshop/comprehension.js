({
    desc: "Go over these questions to deepen your understanding",
    questions: [
        `Why do we need multer?`,
        `What is the return type of makePage? (choices: string, object, function, number, boolean)`,
        `How many endpoints are in the server?`,
        `Is the name attribute of the text input arbitrary chosen? If we change it, do we need to make modifications anywhere else?`
    ]
})